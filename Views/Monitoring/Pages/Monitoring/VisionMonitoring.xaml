<Page x:Class="HyunDaiINJ.Views.Monitoring.Pages.Monitoring.VisionMonitoring"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vision="clr-namespace:HyunDaiINJ.Views.Monitoring.Controls.Vision"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1350"
      Title="VisionMonitoring">
    <Grid Background="#1F1F1F">
        <!-- 전체 레이아웃 그리드 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 헤더 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5" Grid.ColumnSpan="3">
            <materialDesign:PackIcon Foreground="#95C0FF" Kind="CircleOutline" Height="30" Width="40" VerticalAlignment="Center"/>
            <TextBlock Text="비전 1"
                FontSize="18" 
                FontWeight="Bold" 
                Foreground="#95C0FF" 
                Margin="10,0"
                VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 헤더 -->
        <StackPanel Orientation="Horizontal" Grid.Column="9" Grid.Row="0" Margin="5" Grid.ColumnSpan="3">
            <materialDesign:PackIcon Foreground="#95C0FF" Kind="CircleOutline" Height="30" Width="40" VerticalAlignment="Center"/>
            <TextBlock Text="비전 2"
                        FontSize="18" 
                        FontWeight="Bold" 
                        Foreground="#95C0FF" 
                        Margin="10,0"
                        VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 헤더 -->
        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="2" Margin="5" Grid.ColumnSpan="3">
            <materialDesign:PackIcon Foreground="#95C0FF" Kind="CircleOutline" Height="30" Width="40" VerticalAlignment="Center"/>
            <TextBlock Text="비전 3"
                FontSize="18" 
                FontWeight="Bold" 
                Foreground="#95C0FF" 
                Margin="10,0"
                VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 헤더 -->
        <StackPanel Orientation="Horizontal" Grid.Column="9" Grid.Row="2" Margin="5" Grid.ColumnSpan="3">
            <materialDesign:PackIcon Foreground="#95C0FF" Kind="CircleOutline" Height="30" Width="40" VerticalAlignment="Center"/>
            <TextBlock Text="비전 4"
                        FontSize="18" 
                        FontWeight="Bold" 
                        Foreground="#95C0FF" 
                        Margin="10,0"
                        VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 헤더 -->
        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="4" Margin="5" Grid.ColumnSpan="3">
            <materialDesign:PackIcon Foreground="#95C0FF" Kind="CircleOutline" Height="30" Width="40" VerticalAlignment="Center"/>
            <TextBlock Text="비전 5"
                        FontSize="18" 
                        FontWeight="Bold" 
                        Foreground="#95C0FF" 
                        Margin="10,0"
                        VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 첫 번째 Border -->
        <Border Grid.Column="0" Grid.Row="1" BorderBrush="#95C0FF" BorderThickness="4" CornerRadius="5" Margin="5" Grid.ColumnSpan="5">
            <Grid>
                <!-- 내부 콘텐츠 -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <!-- 사진 영역 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 검사 개수 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 버튼 영역 -->
                    </Grid.RowDefinitions>

                    <!-- 사진 영역 -->
                    <Border Grid.Row="0" BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5">
                        <!-- stageVal 나오는 곳-->
                        <vision:VisionProcedure Grid.Row="1"/>
                    </Border>

                    <!-- 검사 개수 -->
                    <TextBlock Grid.Row="1" 
                               Text="검사 개수: 30 / 2000"
                               HorizontalAlignment="Center"
                               Margin="10"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#95C0FF"/>

                    <!-- 버튼 영역 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- 투입 -->
                        <Border Style="{StaticResource InputBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="투입" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold" />
                        </Border>

                        <!-- 비전 -->
                        <Border Style="{StaticResource VisionBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="비전" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>

                        <!-- 완료 -->
                        <Border Style="{StaticResource CompleteBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="완료" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>

                </Grid>
            </Grid>
        </Border>

        <!-- 두 번째 Border -->
        <Border Grid.Column="9" Grid.ColumnSpan="5" Grid.Row="1" BorderBrush="#95C0FF" BorderThickness="4" CornerRadius="5" Margin="5" >
            <Grid>
                <!-- 내부 콘텐츠 -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <!-- 사진 영역 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 검사 개수 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 버튼 영역 -->
                    </Grid.RowDefinitions>

                    <!-- 사진 영역 -->
                    <Border Grid.Row="0" BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5" >
                        <!-- stageVal 나오는 곳-->
                        <vision:VisionProcedure Grid.Row="1"/>
                    </Border>

                    <!-- 검사 개수 -->
                    <TextBlock Grid.Row="1" 
                               Text="검사 개수: 30 / 2000"
                               HorizontalAlignment="Center"
                               Margin="10"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#95C0FF"/>

                    <!-- 버튼 영역 -->
                    <!-- 버튼 영역 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- 투입 -->
                        <Border Style="{StaticResource InputBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="투입" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold" />
                        </Border>

                        <!-- 비전 -->
                        <Border Style="{StaticResource VisionBlinkingStyle}" BorderBrush="Black" BorderThickness="1" Width="60" Height="40" Margin="5">
                            <TextBlock Text="비전" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>

                        <!-- 완료 -->
                        <Border Style="{StaticResource CompleteBlinkingStyle}" BorderBrush="Black" BorderThickness="1" Width="60" Height="40" Margin="5">
                            <TextBlock Text="완료" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 세 번째 Border -->
        <Border Grid.Column="0" Grid.ColumnSpan="5" Grid.Row="3" BorderBrush="#95C0FF" BorderThickness="4" CornerRadius="5" Margin="5" >
            <Grid>
                <!-- 내부 콘텐츠 -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <!-- 사진 영역 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 검사 개수 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 버튼 영역 -->
                    </Grid.RowDefinitions>

                    <!-- 사진 영역 -->
                    <Border Grid.Row="0" BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5">
                        <!-- stageVal 나오는 곳-->
                        <vision:VisionProcedure Grid.Row="1"/>
                    </Border>

                    <!-- 검사 개수 -->
                    <TextBlock Grid.Row="1" 
                               Text="검사 개수: 30 / 2000"
                               HorizontalAlignment="Center"
                               Margin="10"
                               FontSize="16"
                               FontWeight="Bold"/>

                    <!-- 버튼 영역 -->
                    <!-- 버튼 영역 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- 투입 -->
                        <Border Style="{StaticResource InputBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="투입" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold" />
                        </Border>

                        <!-- 비전 -->
                        <Border Style="{StaticResource VisionBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="비전" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>

                        <!-- 완료 -->
                        <Border Style="{StaticResource CompleteBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="완료" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 네 번째 Border -->
        <Border Grid.Column="9" Grid.ColumnSpan="5" Grid.Row="3" BorderBrush="#95C0FF" BorderThickness="4" CornerRadius="5" Margin="5" >
            <Grid>
                <!-- 내부 콘텐츠 -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <!-- 사진 영역 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 검사 개수 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 버튼 영역 -->
                    </Grid.RowDefinitions>

                    <!-- 사진 영역 -->
                    <Border Grid.Row="0" BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5">
                        <!-- stageVal 나오는 곳-->
                        <vision:VisionProcedure Grid.Row="1"/>
                    </Border>

                    <!-- 검사 개수 -->
                    <TextBlock Grid.Row="1" 
                               Text="검사 개수: 30 / 2000"
                               HorizontalAlignment="Center"
                               Margin="10"
                               FontSize="16"
                               FontWeight="Bold"/>

                    <!-- 버튼 영역 -->
                    <!-- 버튼 영역 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- 투입 -->
                        <Border Style="{StaticResource InputBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="투입" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold" />
                        </Border>

                        <!-- 비전 -->
                        <Border Style="{StaticResource VisionBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="비전" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>

                        <!-- 완료 -->
                        <Border Style="{StaticResource CompleteBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="완료" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 다섯 번째 Border -->
        <Border Grid.Column="0" Grid.ColumnSpan="5" Grid.Row="5" BorderBrush="#95C0FF" BorderThickness="4" CornerRadius="5" Margin="5" >
            <Grid>
                <!-- 내부 콘텐츠 -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <!-- 사진 영역 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 검사 개수 -->
                        <RowDefinition Height="Auto"/>
                        <!-- 버튼 영역 -->
                    </Grid.RowDefinitions>

                    <!-- 사진 영역 -->
                    <Border Grid.Row="0" BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5">
                        <!-- stageVal 나오는 곳-->
                        <vision:VisionProcedure Grid.Row="1"/>
                    </Border>

                    <!-- 검사 개수 -->
                    <TextBlock Grid.Row="1" 
                               Text="검사 개수: 30 / 2000"
                               HorizontalAlignment="Center"
                               Margin="10"
                               FontSize="16"
                               FontWeight="Bold"/>

                    <!-- 버튼 영역 -->
                    <!-- 버튼 영역 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- 투입 -->
                        <Border Style="{StaticResource InputBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="투입" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold" />
                        </Border>

                        <!-- 비전 -->
                        <Border Style="{StaticResource VisionBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="비전" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>

                        <!-- 완료 -->
                        <Border Style="{StaticResource CompleteBlinkingStyle}" BorderBrush="Black" BorderThickness="1" CornerRadius="3" Width="60" Height="40" Margin="5">
                            <TextBlock Text="완료" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        
        
        
        <!-- 품질 사진 1 -->
        <Border Grid.Column="5" Grid.ColumnSpan="4"  Grid.Row="1"   BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5,5"  MouseLeftButtonDown="Border_MouseLeftButtonDown" Cursor="Hand">
            <Grid>
                <!-- 품질 이미지 -->
                <Image Source="{Binding CurrentImage}"  HorizontalAlignment="Center" VerticalAlignment="Center"  Stretch="UniformToFill" Grid.Row="1"/>
            </Grid>
        </Border>

        <!-- 품질 사진 2 -->
        <Border Grid.Column="14" Grid.ColumnSpan="4" Grid.Row="1"  BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5,5"  MouseLeftButtonDown="Border_MouseLeftButtonDown" Cursor="Hand">
            <Grid>
                <!-- 품질 이미지 -->
                <Image Source="{Binding CurrentImage}"  HorizontalAlignment="Center" VerticalAlignment="Center"   Stretch="UniformToFill" Grid.Row="1"/>
            </Grid>
        </Border>

        <!-- 품질 사진 3 -->
        <Border Grid.Column="5" Grid.ColumnSpan="4" Grid.Row="3"  BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5,5" MouseLeftButtonDown="Border_MouseLeftButtonDown" Cursor="Hand">
            <Grid>
                <!-- 품질 이미지 -->
                <Image Source="{Binding CurrentImage}"  HorizontalAlignment="Center" VerticalAlignment="Center"   Stretch="UniformToFill" Grid.Row="1"/>
            </Grid>
        </Border>
        
        <!-- 품질 사진 4 -->
        <Border Grid.Column="14" Grid.ColumnSpan="4" Grid.Row="3"  BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5,5" MouseLeftButtonDown="Border_MouseLeftButtonDown" Cursor="Hand">
            <Grid>
                <!-- 품질 이미지 -->
                <Image Source="{Binding CurrentImage}"  HorizontalAlignment="Center" VerticalAlignment="Center"   Stretch="UniformToFill" Grid.Row="1"/>
            </Grid>
        </Border>

        <!-- 품질 사진 5 -->
        <Border Grid.Column="5" Grid.ColumnSpan="4" Grid.Row="5"  BorderBrush="#95C0FF" BorderThickness="2" CornerRadius="5" Margin="5,5" MouseLeftButtonDown="Border_MouseLeftButtonDown" Cursor="Hand">
            <Grid>
                <!-- 품질 이미지 -->
                <Image Source="{Binding CurrentImage}"  HorizontalAlignment="Center" VerticalAlignment="Center"   Stretch="UniformToFill" Grid.Row="1"/>
            </Grid>
        </Border>
    </Grid>
</Page>

